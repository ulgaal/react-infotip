The default configuration of a `<Source>` can be changed using its `config` property.

You do not need to redefine the whole configuration in your `config` property. Defaults will be used for unspecfied values.
